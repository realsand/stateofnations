print("Hello world, from client!")

local UIManager = require(script.Parent.UIManager)

-- Create the UI
UIManager.createUI()

-- Example: Update stats (replace with actual game logic)
UIManager.updateStats(100, 5000, 75)
