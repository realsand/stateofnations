# Client Folder

- `components/`: UI and gameplay components for the client
- `modules/`: Client-only modules
- `util/`: Client-side utilities and helpers
- `init.client.luau`: Main entry point for client scripts