# Server Folder

- `systems/`: Game systems (e.g., economy, combat)
- `modules/`: Server-only modules
- `util/`: Server-side utilities and helpers
- `init.server.luau`: Main entry point for server scripts