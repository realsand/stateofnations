# Shared Folder

- `modules/`: Shared modules (used by both client and server)
- `components/`: Shared components (if any)
- `util/`: Shared utilities and helpers
- `GameState.luau`, `Hello.luau`, `UIManager.luau`: Example shared scripts